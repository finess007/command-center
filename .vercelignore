# Prevent accidentally uploading stale Build Output API artifacts
.vercel/

# Never deploy unencrypted sources (staticrypt source files)
src/

# Local-only deps
node_modules/

# Local build byproducts (safe to exclude)
*.log
.DS_Store
